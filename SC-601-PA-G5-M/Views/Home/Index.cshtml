@{
    ViewBag.Title = "Home Page";
}

<style>
    .hero-section {
        background-size: cover;
        display: flex;
        color: white;
    }

    .hero-text {
        font-size: 10rem;
        font-weight: 900;
        text-transform: uppercase;
        text-shadow: 2px 2px 5px rgba(0,0,0,0.7);
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .hero-text2 {
        font-size: 10rem;
        font-weight: 900;
        text-transform: uppercase;
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
        text-shadow: 2px 2px 5px rgba(0,0,0,0.7);
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .hero-subtext {
        font-size: 5rem;
        font-weight: 700;
        margin-top: 10px;
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
        text-shadow: 1px 1px 3px rgba(0,0,0,0.6);
    }
</style>

<main class="hero-section">
    <div>
        <div class="hero-text">Taller Moto</div>
        <div class="hero-text2">Llantas</div>
        <div class="hero-subtext">Virtual</div>
    </div>
</main>
