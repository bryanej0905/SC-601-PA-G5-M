@model IEnumerable<SC_601_PA_G5_M.Models.DetallePedido>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pedido.EstadoPedido)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Producto.UsuarioId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PrecioUnitario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pedido.EstadoPedido)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Producto.UsuarioId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PrecioUnitario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdDetallePedidop }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdDetallePedidop }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdDetallePedidop })
        </td>
    </tr>
}

</table>
